==========================================
🔄 增强域名监控系统使用说明
==========================================

## 系统功能

✅ 多API查询策略
   - 主RDAP服务器 (Verisign, SIDN, DNS.be等)
   - RDAP Bootstrap服务
   - API Ninjas WHOIS API
   - 自动选择返回信息最多的结果

✅ Playwright爬虫Fallback
   - 当所有API都失败时，自动启动
   - 爬取 https://who.is/ 网站
   - 提取Raw WHOIS数据和结构化信息

✅ DNS记录查询
   - A记录 (IP地址)
   - MX记录 (邮件服务器)
   - TXT记录 (验证token等)
   - NS记录 (名称服务器)

✅ 持续监控模式
   - 定时轮询所有域名
   - 可配置监控间隔
   - 每轮自动保存JSON和CSV结果

==========================================
## 使用方法
==========================================

### 方式1: 使用启动脚本（推荐）

bash start_monitor.sh <csv_file> [interval] [max_iterations]

示例:
  bash start_monitor.sh example_input.csv           # 默认5分钟间隔，无限轮次
  bash start_monitor.sh example_input.csv 600       # 10分钟间隔
  bash start_monitor.sh example_input.csv 300 3     # 5分钟间隔，最多3轮


### 方式2: 直接运行Python脚本

python3 enhanced_domain_monitor.py <csv_file> [选项]

选项:
  --interval SECONDS      监控间隔(秒)，默认300(5分钟)
  --iterations NUM        最大轮数，默认无限
  --api-key KEY          API Ninjas密钥(可选)

示例:
  python3 enhanced_domain_monitor.py example_input.csv --interval 60 --iterations 1


==========================================
## 输出文件
==========================================

每轮监控会生成两个文件:

1. monitor_results_iter{N}_{timestamp}.json
   - 完整的JSON格式数据
   - 包含所有字段和原始响应

2. monitor_results_iter{N}_{timestamp}.csv
   - CSV格式，可用Excel打开
   - 包含: 域名、成功状态、数据源、注册信息、DNS记录等


==========================================
## CSV字段说明
==========================================

- domain: 域名
- success: 是否成功获取信息 (Y/N)
- data_source: 数据来源 (rdap_primary/rdap_bootstrap/api_ninjas/whois_scraper)
- registrar: 注册商
- registry: 注册局
- creation_date: 创建日期
- expiry_date: 过期日期
- rdap_nameservers: 从RDAP获取的名称服务器
- dns_a_records: DNS A记录(IP地址)
- dns_mx_records: DNS MX记录(邮件服务器)
- dns_txt_records: DNS TXT记录
- dns_ns_records: DNS NS记录(名称服务器)
- timestamp: 查询时间戳


==========================================
## 监控策略
==========================================

对每个域名:

1. 并行查询3个API
   ├─ 主RDAP服务器
   ├─ RDAP Bootstrap
   └─ API Ninjas

2. 比较结果，选择信息最多的

3. 如果所有API都失败
   └─ 启动Playwright爬虫
      └─ 访问 https://who.is/
         └─ 提取Raw WHOIS和结构化数据

4. 查询DNS记录 (A/MX/TXT/NS)

5. 合并所有数据并保存


==========================================
## 停止监控
==========================================

按 Ctrl+C 可随时安全停止监控
系统会保存当前轮次的数据后退出


==========================================
## 依赖要求
==========================================

- Python 3.8+
- httpx
- dnspython  
- playwright
- Chromium浏览器 (playwright会自动安装)


==========================================
## 示例场景
==========================================

### 场景1: 快速检查一次
python3 enhanced_domain_monitor.py example_input.csv --iterations 1

### 场景2: 每小时监控一次，共监控24小时
python3 enhanced_domain_monitor.py example_input.csv --interval 3600 --iterations 24

### 场景3: 持续监控，每5分钟一次
python3 enhanced_domain_monitor.py example_input.csv --interval 300

### 场景4: 快速频繁监控，用于测试
python3 enhanced_domain_monitor.py example_input.csv --interval 60 --iterations 5


==========================================
## 成功率提升
==========================================

相比单一API查询:
- 原始方式: ~26% 成功率
- 多API策略: ~40-50% 成功率
- 加上爬虫: ~70-80% 成功率
- 加上DNS查询: ~90%+ 至少获取部分信息


==========================================
## 注意事项
==========================================

1. 爬虫会增加处理时间(每个域名约5-10秒)
2. 请遵守网站的使用条款和robots.txt
3. 建议不要设置过短的监控间隔(建议≥5分钟)
4. API Ninjas需要密钥，但不是必需的


==========================================
创建日期: 2025-11-29
版本: 1.0
==========================================

